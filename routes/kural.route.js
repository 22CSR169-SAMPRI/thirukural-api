import express from 'express'
const router=express.Router()
router.get("/kural",()=>{});
router.post("/kural",()=>{});
router.put("/kural/:id",()=>{});
router.delete("/kural/:id",()=>{});
export default router;